{{- if .Values.notificationService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-chatbot.fullname" . }}-notification
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
    app: notification
spec:
  replicas: {{ .Values.notificationService.replicaCount }}
  selector:
    matchLabels:
      {{- include "todo-chatbot.selectorLabels" . | nindent 6 }}
      app: notification
  template:
    metadata:
      labels:
        {{- include "todo-chatbot.selectorLabels" . | nindent 8 }}
        app: notification
      {{- if .Values.dapr.enabled }}
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "{{ .Values.dapr.notificationService.appId }}"
        dapr.io/app-port: "{{ .Values.dapr.notificationService.appPort }}"
        dapr.io/config: "{{ .Values.dapr.config }}"
        dapr.io/log-level: "{{ .Values.dapr.logLevel }}"
        {{- if .Values.dapr.notificationService.enableMetrics }}
        dapr.io/enable-metrics: "true"
        dapr.io/metrics-port: "{{ .Values.dapr.notificationService.metricsPort }}"
        {{- end }}
      {{- end }}
    spec:
      securityContext:
        {{- toYaml .Values.notificationService.podSecurityContext | nindent 8 }}
      containers:
        - name: notification
          securityContext:
            {{- toYaml .Values.notificationService.securityContext | nindent 12 }}
          image: "{{ .Values.notificationService.image.repository }}:{{ .Values.notificationService.image.tag }}"
          imagePullPolicy: {{ .Values.notificationService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.notificationService.service.targetPort }}
              protocol: TCP
          env:
            # Service configuration
            - name: SERVICE_NAME
              value: "notification-service"
            - name: LOG_LEVEL
              value: {{ .Values.global.logLevel | quote }}
            # Dapr configuration
            {{- if .Values.dapr.enabled }}
            - name: DAPR_ENABLED
              value: "true"
            - name: DAPR_HTTP_PORT
              value: "{{ .Values.dapr.httpPort }}"
            - name: PUBSUB_NAME
              value: "{{ .Values.dapr.pubsubName }}"
            - name: REMINDERS_TOPIC
              value: "{{ .Values.kafka.topics.reminders }}"
            {{- else }}
            - name: DAPR_ENABLED
              value: "false"
            {{- end }}
            # Database URL for storing processed events (idempotency)
            - name: NEON_DB_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "todo-chatbot.fullname" . }}-db-secret
                  key: NEON_DB_URL
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.notificationService.resources | nindent 12 }}
      {{- with .Values.notificationService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.notificationService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.notificationService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
